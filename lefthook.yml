pre-push:
  commands:
    lintfix:
      run: pnpm lint:fix
      stage_fixed: true

commit-msg:
  commands:
    git-conventional-commits:
      run: pnpm git-conventional-commits commit-msg-hook {1}
