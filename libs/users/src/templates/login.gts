
import LoginFormComponent from "#src/components/login-form.gts";
import type { TOC } from "@ember/component/template-only";

export default <template>
    <LoginFormComponent />
</template> as TOC<object>;
